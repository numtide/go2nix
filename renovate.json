{
  "$schema": "https://docs.renovatebot.com/renovate-schema.json",
  "extends": [
    "config:recommended"
  ],
  "ignoreDeps": [
    "github.com/nix-community/go-nix"
  ],
  "$comment": "github.com/nix-community/go-nix is pinned through the go.mod replace to github.com/numtide/go-nix; bump the replace, not the require."
}
